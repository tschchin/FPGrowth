# Frequent Patterns
### FPGrowth Implement
## Command
```
python main.py [ min_support ] [ input_file ] [ output_file ]
```
standard module:
    sys
    collections (Counter)
    itertools (combinations)
    math (floor)
